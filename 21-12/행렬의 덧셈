def solution(arr1, arr2):
    len_y = len(arr1)
    len_x = len(arr1[0])
    answer = []
    for i in range(0, len_y):
        tmp = []
        for j in range(0, len_x):
            tmp.append(arr1[i][j] + arr2[i][j])
        answer.append(tmp)
    return answer
