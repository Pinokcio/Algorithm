def solution(n):
    answer = []
    n = str(n)
    length = len(n)
    answer = [n[0]]
    for i in range(1, length):
        answer.append("+"+n[i])
    answer = "".join(answer)
    return eval(answer)
